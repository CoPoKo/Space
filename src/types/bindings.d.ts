export { };

declare global {
  const AUTH_PAGE: string;
  const MY_REFERER: string;
  const SpaceName: string;
  const SpacePassword: string;
  const Telegraf_BOT_TOKEN: string;
  const Telegraf_BOT_WEBHOOK: string;
  const reCAPTCHA_CLIENT: string;
  const reCAPTCHA_SERVER: string;
  const ACCOUNTID: string;
  const ZONEID: string;
  const AUTHEMAIL: string;
  const AUTHKEY: string;
  const WORKERNAME: string;
  const WORKERROUTE: string;
  const SpaceKV: KVNamespace;
}